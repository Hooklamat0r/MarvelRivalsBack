nelmio_cors:
    defaults:
        allow_origin: ['http://app.marvelrivals.local']
        allow_methods: ['GET', 'POST', 'OPTIONS', 'PUT', 'PATCH', 'DELETE']
        allow_headers: ['Content-Type', 'Authorization']
        allow_credentials: true
    paths:
        '^/api': ~
